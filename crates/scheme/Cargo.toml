[package]
name = "scheme"
description = "The libscheme REPL driver (Rust port of src/main.c)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "scheme"
path = "src/main.rs"

[dependencies]
libscheme = { path = "../libscheme" }
